apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-gw-infra.fullname" . }}-test-connection"
  labels:
    {{ include "helm-gw-infra.labels" . | nindent 4 }}
spec:
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args:  ['{{ include "helm-gw-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
